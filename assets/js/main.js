const API_BASE = '/api/v1/posts/page';
let currentPage = 1;
let totalPages = 1;

  try {
    totalPages = data.totalPages;
    return data.posts;
    return [];
  }
}

    currentPage++;
  }
}

    const slug = post.slug 
    const title = post.title;
    const excerpt = post.excerpt 
}

document.addEventListener('DOMContentLoaded', async (
